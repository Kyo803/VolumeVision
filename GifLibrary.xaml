<Window x:Class="VolumeOSD.GifLibrary"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GIF Library" Width="900" Height="640"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Topmost="True"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Border x:Name="Shell" CornerRadius="20" Background="#0B0B0E" BorderBrush="#1E1E24" BorderThickness="1"
            RenderTransformOrigin="0.5,0.5" MouseLeftButtonDown="Shell_Drag">
        <Border.RenderTransform>
            <ScaleTransform x:Name="ShellScale" ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>
        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="GIF Library" FontSize="20" FontWeight="SemiBold" Foreground="#F4F4F7"/>
                    <TextBlock x:Name="FolderHint" Foreground="#5A5A64" FontSize="11" Margin="0,2,0,0"/>
                </StackPanel>
                <Button x:Name="CloseBtn" Content="✕" Width="28" Height="28" HorizontalAlignment="Right"
                        VerticalAlignment="Top" Style="{StaticResource GhostPillButton}" Padding="0" Click="Close_Click"/>
            </Grid>

            <!-- Toolbar -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
                <Button Content="Add GIFs…" Style="{StaticResource PillButton}" Margin="0,0,8,0" Click="Add_Click"/>
                <Button Content="Open folder" Style="{StaticResource GhostPillButton}" Margin="0,0,8,0" Click="OpenFolder_Click"/>
                <Button Content="Refresh" Style="{StaticResource GhostPillButton}" Margin="0,0,8,0" Click="Refresh_Click"/>
                <Button Content="None (clear)" Style="{StaticResource GhostPillButton}" Click="Clear_Click"/>
            </StackPanel>

            <!-- Gallery -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <WrapPanel x:Name="Items" Orientation="Horizontal"/>
            </ScrollViewer>

            <!-- Status -->
            <TextBlock Grid.Row="3" x:Name="StatusText" Foreground="#5A5A64" FontSize="11" Margin="2,10,0,0"/>
        </Grid>
    </Border>
</Window>
