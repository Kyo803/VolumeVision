<UserControl x:Class="VolumeOSD.Controls.Timeline"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:VolumeOSD.Controls">
    <Border Background="#0E0E12" BorderBrush="#22222A" BorderThickness="1" CornerRadius="12" Padding="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ===== Transport ===== -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="FirstBtn" Content="⏮" Style="{StaticResource GhostPillButton}" Width="30" Margin="0,0,4,0" Click="First_Click"/>
                    <Button x:Name="PrevBtn" Content="◀" Style="{StaticResource GhostPillButton}" Width="30" Margin="0,0,4,0" Click="Prev_Click"/>
                    <Button x:Name="PlayBtn" Content="▶" Style="{StaticResource PillButton}" Width="40" Margin="0,0,4,0" Click="Play_Click"/>
                    <Button x:Name="StopBtn" Content="■" Style="{StaticResource GhostPillButton}" Width="30" Margin="0,0,4,0" Click="Stop_Click"/>
                    <Button x:Name="NextBtn" Content="▶" Style="{StaticResource GhostPillButton}" Width="30" Margin="0,0,4,0" Click="Next_Click"/>
                    <Button x:Name="LastBtn" Content="⏭" Style="{StaticResource GhostPillButton}" Width="30" Click="Last_Click"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock x:Name="FrameLabel" Text="0 / 0" Foreground="#9A9AA5" FontSize="11" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button x:Name="AddBtn" Content="+ Frame" Style="{StaticResource GhostPillButton}" Margin="0,0,4,0" Click="Add_Click"/>
                    <Button x:Name="DupeBtn" Content="Duplicate" Style="{StaticResource GhostPillButton}" Margin="0,0,4,0" Click="Dupe_Click"/>
                    <Button x:Name="DelBtn" Content="Delete" Style="{StaticResource GhostPillButton}" Margin="0,0,4,0" Click="Del_Click"/>
                    <Button x:Name="UndoBtn" Content="Undo" Style="{StaticResource GhostPillButton}" Click="Undo_Click"/>
                </StackPanel>
            </Grid>

            <!-- ===== Ruler + cells (scroll together) ===== -->
            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <StackPanel Orientation="Vertical">
                    <ItemsControl x:Name="Ruler" Height="14">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Tick}" Width="34" FontSize="9" Foreground="#55555E"
                                           TextAlignment="Left" Margin="0,0,0,0"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <ListBox x:Name="Cells" Height="46" Background="Transparent" BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Disabled"
                             SelectionChanged="Cells_SelectionChanged">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="1,0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="b" CornerRadius="6" BorderThickness="1.5"
                                                    BorderBrush="#2C2C33" Background="#17171C" Padding="1">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="b" Property="BorderBrush" Value="#FFFFFF"/>
                                                    <Setter TargetName="b" Property="Background" Value="#242430"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="b" Property="BorderBrush" Value="#6A6A74"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Image Source="{Binding Thumb}" Width="30" Height="40" Stretch="UniformToFill"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </ScrollViewer>

            <!-- ===== Info ===== -->
            <TextBlock Grid.Row="2" x:Name="InfoText" Text="No frames" Foreground="#55555E" FontSize="10" Margin="2,6,0,0"/>
        </Grid>
    </Border>
</UserControl>
