<UserControl x:Class="VolumeOSD.Controls.LiquidGlassPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             ClipToBounds="False">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="GlassEdgeBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#66FFFFFF" Offset="0.0"/>
            <GradientStop Color="#14FFFFFF" Offset="0.4"/>
            <GradientStop Color="#08FFFFFF" Offset="1.0"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="GlassTintBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#33FFFFFF" Offset="0.0"/>
            <GradientStop Color="#14FFFFFF" Offset="1.0"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="SpecularBrush" StartPoint="0,0" EndPoint="1,0.3">
            <GradientStop Color="#55FFFFFF" Offset="0.0"/>
            <GradientStop Color="#00FFFFFF" Offset="0.5"/>
        </LinearGradientBrush>
        <RadialGradientBrush x:Key="CursorGlow" GradientOrigin="0.5,0.5" Center="0.5,0.5" RadiusX="0.6" RadiusY="0.6">
            <GradientStop Color="#40FFFFFF" Offset="0"/>
            <GradientStop Color="#00FFFFFF" Offset="1"/>
        </RadialGradientBrush>
    </UserControl.Resources>
    <Grid x:Name="Root">
        <!-- Elevation shadow -->
        <Border CornerRadius="24" Background="Black" Opacity="0.18" Margin="0,4,0,-4">
            <Border.Effect><BlurEffect Radius="18"/></Border.Effect>
        </Border>
        <!-- Backdrop blur host (filled by parent via background visual snapshot when needed) -->
        <Border x:Name="BlurHost" CornerRadius="24" ClipToBounds="True"/>
        <!-- Adaptive tint wash -->
        <Border x:Name="TintLayer" CornerRadius="24" Background="{StaticResource GlassTintBrush}"/>
        <!-- Static specular top strip -->
        <Border CornerRadius="24,24,0,0" Height="40" VerticalAlignment="Top"
                Background="{StaticResource SpecularBrush}" Opacity="0.6"/>
        <!-- Cursor-reactive glow (over tint, under edge) -->
        <Border x:Name="GlowLayer" CornerRadius="24" Background="{StaticResource CursorGlow}"
                Opacity="0" IsHitTestVisible="False"/>
        <!-- Gradient edge -->
        <Border CornerRadius="24" BorderThickness="1" BorderBrush="{StaticResource GlassEdgeBrush}"/>
        <!-- Host content -->
        <ContentPresenter Margin="{Binding ContentMargin, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
    </Grid>
</UserControl>
